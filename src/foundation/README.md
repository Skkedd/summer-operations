# Vendored Deep Site Foundation

This directory is the 0.1.2 runtime source snapshot from the sibling `deep-site-foundation` repository at commit `936db82310ced8cc274dc63474ec9f8c265f8ada`. The copy keeps this product's build and deployment independent while private-package install credentials for deployment are unverified. Update all copies from the canonical source together, run its contract tests, and record the new source commit. Replace this source copy with a pinned package when publication is available to each deployment.
